# Rename this file to local_config.yaml and use it to specify persistent configuration options for
# the local machine this is running on.
trainer:
  accelerator: auto
  devices: auto
  strategy: auto
env:
  mlflow_tracking_uri: mlruns
  data_root: data
